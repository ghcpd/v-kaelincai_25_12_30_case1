# issue_project_fixed

This is the corrected version of the Shift Scheduler project.

Changes:
- Fixed same-day shift validation so employees cannot have multiple shifts on the same day.

All tests pass:

```
pytest -v
12 passed
```
